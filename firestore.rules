rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // Allow full operational access for riders, admin dispatch, routes, and trips
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
